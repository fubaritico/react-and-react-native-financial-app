/** Default donut chart size for the BudgetOverview component. */
export const CHART_SIZE = 240

/** Smaller donut chart size for compact layouts (mobile vertical with title). */
export const CHART_SIZE_COMPACT = 200

/** Tablet breakpoint width in dp for responsive layout switch (768dp). */
export const TABLET_BREAKPOINT = 768
