/** Web-only classes for PasswordInput (hover, transition, cursor) */
export const web = {
  /** Toggle button hover feedback */
  toggle: 'ml-2 hover:opacity-70 transition-opacity cursor-pointer',
} as const
