import type { ReactNode } from 'react'

/** Props for the TableRow sub-component (internal). */
export interface ITableRowProps {
  children?: ReactNode
  /** Enable hover highlight on web */
  enableHover?: boolean
}
