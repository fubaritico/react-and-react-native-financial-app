/** Default chart diameter in pixels */
export const DEFAULT_SIZE = 240

/** Ratio of inner radius to outer radius (controls donut hole size) */
export const INNER_RADIUS_RATIO = 0.65

/** Opacity of the inner highlight ring overlay */
export const HIGHLIGHT_OPACITY = 0.2

/** How much of the ring width the highlight covers (from the inner edge) */
export const HIGHLIGHT_RATIO = 0.3

/** Start angle offset — -90° puts the first segment at 12 o'clock */
export const START_ANGLE_DEG = -90
