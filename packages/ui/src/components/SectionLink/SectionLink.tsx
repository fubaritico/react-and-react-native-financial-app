/** Props for the SectionLink component. */
export interface ISectionLinkProps {
  /** Link label text (e.g., "See Details", "View All"). */
  label: string
  /** Callback fired when the link is pressed. */
  onPress: () => void
}
