import { View } from 'react-native'

/** Placeholder screen — never displayed. Sign-out is handled by tabPress listener in _layout.tsx. */
export default function SignOutScreen() {
  return <View />
}
