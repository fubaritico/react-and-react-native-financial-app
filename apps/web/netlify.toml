# @netlify/vite-plugin-react-router handles SSR function generation,
# static asset publishing, and redirects automatically.
# Only the build command and publish directory need to be specified.

[build]
  command = "react-router build"
  publish = "build/client"
